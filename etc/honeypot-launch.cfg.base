#Change the below to yes if you are using authbind to listen to port 443
AUTHBIND_ENABLED=no

# To override the default virtual enviroment, either set HONEYPOT_VIRTUAL_ENV or
# activate it before starting the honeypot
HONEYPOT_VIRTUAL_ENV=ipphoney-env
